/**
 * @deprecated Use useAuth from '@/components/providers/AuthProvider' instead
 * This file exists only for backwards compatibility
 */

// Re-export useAuth from AuthProvider to maintain backwards compatibility
export { useAuth } from '../components/providers/AuthProvider'

// Re-export User type for convenience
export type { User } from '../components/providers/AuthProvider'
