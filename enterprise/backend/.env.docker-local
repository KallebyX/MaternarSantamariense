# Banco de dados PostgreSQL local via Docker
DATABASE_URL="postgresql://maternar:maternar123@localhost:5432/maternarsm"

# JWT
JWT_SECRET="maternar-jwt-secret-2024"
REFRESH_TOKEN_SECRET="maternar-refresh-secret-2024"

# Configurações do servidor
PORT=4000
NODE_ENV=development

# CORS
CORS_ORIGINS="http://localhost:3000,http://localhost:4000,http://192.168.100.3:3000"

# Redis local via Docker
REDIS_URL="redis://localhost:6379"
DISABLE_REDIS=false

# Email (opcional para desenvolvimento)
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="noreply@maternarsm.local"

# Logs
LOG_LEVEL=debug